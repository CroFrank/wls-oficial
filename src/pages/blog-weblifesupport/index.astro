---
import { getCollection } from "astro:content"
import Layout from "../../layouts/Layout.astro"

const posts = await getCollection("blog")
---

<Layout
  metaTitle="Blog o Web Tehnologijama, IT Trendovima i Umjetnoj Inteligenciji | WebLifeSupport"
  metaDescription="Pratite najnovije članke o web razvoju, IT tehnologijama, umjetnoj inteligenciji i digitalnim trendovima. Istražite znanje, savjete i uvide iz svijeta tehnologije."
>
  <section class="mx-auto px-4 pb-12 bg-gray-100 w-full min-h-screen py-40">
    <h1 class="text-3xl lg:text-6xl text-center text-gray-900 mb-6">
      WLS Blog
    </h1>
    <h2
      class="text-lg lg:text-xl text-center text-gray-900 mb-12 max-w-4xl mx-auto"
    >
      Teme o web razvoju, IT tehnologijama, umjetnoj inteligenciji i digitalnim
      trendovima. Informacije, savjeti i uvid u svijet tehnologije.
    </h2>
    <div class="max-w-7xl mx-auto px-4 lg:px-10">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          posts.map((post) => (
            <a
              href={`/blog-weblifesupport/${post.id}/`}
              class="bg-white rounded-2xl shadow-md hover:shadow-xl transition-shadow duration-300 overflow-hidden flex flex-col min-w-72"
            >
              <img
                src={post.data.image?.src}
                alt={post.data.image?.alt || "Blog post image"}
                class="w-full h-48 object-cover"
              />
              <div class="p-6 flex flex-col flex-grow">
                <h2 class="text-xl font-semibold text-gray-800 mb-2">
                  {post.data.title}
                </h2>
                <p class="text-gray-600 text-sm line-clamp-3 mb-4">
                  {post.data.description}
                </p>
                <span class="mt-auto text-xs text-gray-400">
                  {post.data.pubDate.toLocaleDateString("hr-HR")}
                </span>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </section>
</Layout>
