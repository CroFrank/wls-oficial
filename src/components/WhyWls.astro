---
import { whyUs } from "../data/reusable"
---

<section
  class="pb-20 pt-10 lg:pt-20 px-6 md:px-12 lg:px-24 bg-gradient-to-b from-gray-50 to-white"
>
  <div class="container mx-auto text-center">
    <h2 class="text-4xl md:text-5xl text-gray-900 mb-6">
      Zašto odabrati <span class="text-[#ff9900]">Web Life Support</span>?
    </h2>
    <h3 class="text-sm md:text-lg text-gray-900 mb-12 max-w-4xl mx-auto">
      OPREZ!!! Spoj dizajna, tehnologije i korisničkog iskustva — može dovesti
      do naglog povečanja broja vaših klijenata.
    </h3>
    <div class="grid md:grid-cols-2 gap-8 text-left">
      {
        whyUs.map((item) => (
          <div class="p-6 bg-white rounded-2xl shadow-md hover:shadow-xl transition duration-300">
            <h4 class="text-xl font-semibold text-gray-800 mb-2">
              {item.title}
            </h4>
            <p class="text-gray-800 leading-relaxed">{item.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
