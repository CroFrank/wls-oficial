---
import { whyUs } from "../data/reusable"
---

<section
  class="pb-16 px-4 md:px-8 lg:px-20 pt-40 bg-gradient-to-b from-gray-50 to-white"
>
  <div class="max-w-6xl mx-auto text-center">
    <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-12">
      Zašto odabrati <span class="text-[#ff9900]">Web Life Support</span>?
    </h2>
    <div class="grid md:grid-cols-2 gap-8 text-left">
      {
        whyUs.map((item) => (
          <div class="p-6 bg-gray-50 rounded-2xl shadow-md hover:shadow-xl transition duration-300">
            <h3 class="text-xl font-semibold text-gray-800 mb-2">
              {item.title}
            </h3>
            <p class="text-gray-600 leading-relaxed">{item.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
