---
import { weOffer } from "../data/reusable"
---

<section
  class="bg-gradient-to-b from-black to-gray-50 pb-20 px-6 md:px-12 lg:px-24"
>
  <div class="max-w-6xl mx-auto text-center">
    <h2 class="text-4xl md:text-5xl text-white mb-6">
      Što nudimo<span class="text-[#ff9900]">?</span>
    </h2>
    <p class="text-sm md:text-lg text-gray-100 mb-12 max-w-3xl mx-auto">
      Usko smo specijalizirani za izradu web stranica. Ako trebate web stranicu
      za vaše poslovanje, hobi ili nešto treće – na pravom ste mjestu. Nudimo
      izradu maksimalno jednostavnih, a opet modernih i funkcionalnih web
      stranica, kao i onih kompleksnih.
    </p>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 text-left">
      {
        weOffer.map((service) => (
          <div class="p-6 bg-gray-50 rounded-2xl shadow-md hover:shadow-xl transition duration-300">
            <h3 class="text-xl text-[#ff9900] mb-2">{service.title}</h3>
            <p class="text-gray-600">{service.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
