---
import { weOffer } from "../data/reusable"
import { InfoCard } from "./InfoCard"
---

<section
  class="bg-gradient-to-b from-black to-gray-50 pb-40 px-6 md:px-12 lg:px-24"
>
  <div class="container mx-auto text-center">
    <h2 class="text-4xl md:text-5xl text-white mb-6">
      Što nudimo<span class="text-[#ff9900]">?</span>
    </h2>
    <h3 class="text-sm md:text-lg text-gray-100 mb-12 max-w-4xl mx-auto">
      Web stranice koje zaista prodaju! Želite više interakcije, više prodaje i
      web stranicu koja radi za vas? Na pravom ste mjestu.
    </h3>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 text-left">
      {
        weOffer.map((service) => (
          <InfoCard
            title={service.title}
            description={service.description}
            client:load
          />
        ))
      }
    </div>
  </div>
</section>
